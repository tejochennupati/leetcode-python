class Solution(object):
    def toLowerCase(self, s):
        result = ""

        for ch in s:

            if 65 <= ord(ch) <= 90:
                result += chr(ord(ch) + 32)
            else:
                result += ch

        return result
        
